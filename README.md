# Rasa · Domain · Soccer · Goalkeeping

**Canonical name:** `rasa.domain.soccer.goalkeeping` (the **first sub-namespaced domain** — dotted `soccer.<specialty>` hierarchy)
**Repo / folder:** `domain-soccer-goalkeeping` (dashed form of the dotted name)
**Alias:** `rasa.domain.soccer-goalkeeping` (flat-form discoverability)
**Kind:** `domain` (canon Spec §6)
**Contract:** Element Contract v1.3.0
**Version:** 0.1.0 (initial; scaffolded from `rasa.domain.core`)
**Status:** v0.1.0 Phase 1 shell — canon-shaped, no goalkeeping content yet.

## What this is

RasaOS Domain · Soccer Goalkeeping. Coaching foundation for soccer goalkeeping — technique, shot-stopping, distribution, positioning, training drills, match preparation, and keeper development across age/level.

## Forked from

`rasa.domain.core` v1.1.0. See the template's `content/SHAPE.md` for pattern choices. Delete that file once you pick a pattern and replace with `content/README.md`.

## TODO

- Fill in this README with the real description, shape choice, file layout, usage.
- Author this Element's specific `CLAUDE.md` (the inherited one is the template's generic).
- Populate `content/` per chosen shape pattern.
- See CHANGELOG.md for the full first-ship TODO list.

## See also

- `~/rAI/rasa-os/elements/domain-core/` — the template this Element forked from
- Canon Spec §6 — the `domain` kind definition
- `~/rAI/rasa-os/elements/REGISTRY.md` — register this Element here on first ship
